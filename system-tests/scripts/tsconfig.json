{
  "extends":"../tsconfig.json",
  "compilerOptions": {
      /** NOTE: only needed to run data-context package */
    "experimentalDecorators": true
  },
  /** NOTE: only needed to run data-context package */
  "include": ["../../packages/data-context/src/**/*.ts"],
}